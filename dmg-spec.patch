--- gajim-source/mac/gajim.spec	2025-12-29 17:09:50.648185526 +0100
+++ gajim-source/mac/gajim.spec	2025-12-29 17:33:09.938062870 +0100
@@ -12,6 +12,23 @@
 
 import sys
 import glob
+import platform
+
+hidden_libs = ['libsoup-*.dylib', 'libgtksourceview-*.dylib']
+
+# Get system arch
+if platform.machine() == 'x86_64':
+	lib_path = '/usr/local/lib/'
+elif platform.machine() == 'arm64':
+	lib_path = '/opt/homebrew/lib/'
+
+hidden_binaries = []
+
+for lib_name in hidden_libs:
+	lib_file = glob.glob(lib_path + lib_name)
+	print("LIB FOUND:", lib_file)
+	hidden_binaries.append((lib_file, '.'))
+
 
 sys.path.insert(0, os.path.join(cwd))
 
@@ -23,7 +40,7 @@
 
 a = Analysis(['launch.py'],
              pathex=[cwd],
-             binaries=[],
+             binaries=hidden_binaries,
              datas=[('gajim', 'gajim')],
              hiddenimports=hiddenimports,
              hookspath=[],
